# （可選）拖拉開檔支援：tkinterdnd2

本工具的拖拉開檔功能是「可選」。

如果你想在 exe 內也支援拖拉（TkDND），可以把 `requirements_optional.txt` 的內容複製回 `requirements.txt`：

- 將 `requirements_optional.txt` 內容複製到 `requirements.txt`
- 重新跑 GitHub Actions 產出新的 exe

注意：即使不裝 tkinterdnd2，程式照樣可用，只是少了拖拉開檔。
